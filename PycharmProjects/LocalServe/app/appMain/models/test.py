import jwt
# print(dir(jwt.exceptions))
# import jwt

# Print out the module path
print("JWT module path:", jwt.__file__)

# Check available exceptions
print("Available exceptions:", dir(jwt.exceptions))
